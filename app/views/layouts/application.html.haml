!!! 5
%html
  %head
    %title My Diet
    = stylesheet_link_tag "main", :media => "all"
    = javascript_include_tag "application"
    = csrf_meta_tags
  %body
    %section#container
      %header
        %h1 My Diet
        - unless content_for(:header).empty?
          = yield :header
      %section#content
        - flash.each do |name, msg|
          - if msg.is_a?(String)
            %section{:class => name}= msg
        = yield
      %footer
